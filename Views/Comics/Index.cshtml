@model List<FBZ.Encyclopedia.Web.Models.ComicRecord>

<h2>Fantasy B’zaar Comic Encyclopedia</h2>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Character</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var comic in Model)
        {
            <tr>
                <td>@comic.Title</td>
                <td>@comic.Character</td>
            </tr>
        }
    </tbody>
</table>